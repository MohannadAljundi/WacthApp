<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.watch">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.bluetooth" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Bluetooth.BluetoothActivity"></activity>
        <activity android:name=".Bus.BusEditProfileActivity" />
        <activity android:name=".Student.StudentEditProfileActivity" />
        <activity android:name=".School.SchoolEditProfileActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Test.Maps2Activity"
            android:label="@string/title_activity_maps2" />
        <activity android:name=".Test.AddingInfoActivity" />
        <activity android:name=".MapMarkerTestActivity" />
        <activity android:name=".Student.HealthyActivityStateStudent" />
        <activity android:name=".LocationGetAutoActivity" />
        <activity android:name=".LocationSetAutoActivity" />
        <activity android:name=".DatabaseTest" />
        <activity android:name=".Bus.StudentHomeActivity" />
        <activity android:name=".Bus.StudentOnBoardActivity" />
        <activity android:name=".Bus.ScanQRActivity" />
        <activity android:name=".School.StudentLocationActivity" />
        <activity android:name=".School.BusTrafficActivity" />
        <activity android:name=".Bus.BusLoginActivity" />
        <activity android:name=".Student.StudenLoginActivity" />
        <activity android:name=".Bus.BusProfileActivity" />
        <activity android:name=".School.AttendanceSchoolActivity" />
        <activity android:name=".School.HealthStateSchoolActivity" />
        <activity android:name=".School.AddNewBusActivity" />
        <activity android:name=".School.AddNewStudentActivity" />
        <activity android:name=".School.AddNewMemberSchoolActivity" />
        <activity android:name=".School.SchoolProfileActivity" />
        <activity android:name=".School.SchoolLoginActivity" />
        <activity android:name=".Student.NonAttendanceStudentActivity" />
        <activity android:name=".Student.ViewMapStudentActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".Student.BusMapActivity" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="om.google.android.gms.vision,DEPENDENCIES"
            android:value="barcode" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Student.NavigateToStudentActivity" />
        <activity android:name=".Student.StudentProfileActivity" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".School.SignUPSchoolActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity"></activity>
    </application>

</manifest>